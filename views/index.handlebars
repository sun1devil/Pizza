<h1>Pizzas!</h1>

<h2>Pizzas that are not gobbled!</h2>

<ul>
  {{#each Pizzas}}
    {{#unless gobbled}}
      {{> Pizzas/Pizza-block sleep=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Pizzas that are gobbled!</h2>

<ul>
  {{#each Pizzas}}
    {{#if gobbled}}
      {{> Pizzas/Pizza-block sleep=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Pizza</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Pizza Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="slee">gobbled?</label><br>
    <input type="radio" name="gobbled" value="1" checked> gobbled!<br>
    <input type="radio" name="gobbled" value="0"> not gobbled
  </div>

  <button type="submit">Add Pizza</button>
</form>
