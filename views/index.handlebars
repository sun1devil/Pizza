<h1>Pizzas!</h1>

<h2>Available Pizzas:</h2>

<ul>
  {{#each pizzas}}
    {{#unless gobbled}}
      {{> pizzas/pizza-block sleep=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Gobbled Pizzas:</h2>

<ul>
  {{#each pizzas}}
    {{#if gobbled}}
      {{> pizzas/pizza-block sleep=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Pizza</h2>
<form class="create-form">

  <div class="form-group">
    <label for="pz">Pizza Name:</label>
    <input type="text" id="pz" name="name">
  </div>

  <div class="form-group">
    <label for="eaten"></label><br>
    <input type="radio" name="gobbled" value="0"> Available
    <input type="radio" name="gobbled" value="1" checked> gobbled!<br>
    
  </div>

  <button type="submit">Add Pizza</button>
  
</form>
